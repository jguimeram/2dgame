import { Player } from "./Entities/Player.js";
import { Boss } from "./Entities/Boss.js";
export class Render{

    public init(){
     /*    const player = new Player(new Vector2(300, this.canvas.height/2), "red", 75)
        player.draw(this.ctx)
        const boss = new Boss(new Vector2(1200,this.canvas.height/2), "yellow", 150)
        boss.draw(this.ctx) */
    }

}